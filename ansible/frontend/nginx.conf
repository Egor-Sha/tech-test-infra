server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ =404;
    }

    location /clock {
        proxy_pass http://clocker-backend:8000/clock;
    }
}
